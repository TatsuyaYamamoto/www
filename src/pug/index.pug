block vars
  - var lang = "ja"
  - var page_file_name = "index";

include include/_navbar

doctype html
html(lang=`${lang}`)
  head
    include include/_head
    link(rel="stylesheet" href=`/css/${page_file_name}.css`)
  body
    header
      +navbar(true, true)

    main
      //include include/_promotion_cards

      div#about
        include include/_about

    footer
      include include/_footer

    script(src=`/js/${page_file_name}.js`)
